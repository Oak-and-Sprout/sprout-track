/**
 * Styles for the ChartDataTable component
 *
 * The entire table is wrapped in Tailwind's `sr-only` class so it is
 * visually hidden and takes no layout space, while remaining fully
 * available to screen readers.
 */
export const chartDataTableStyles = {
  table: "sr-only",
} as const;
