import type { Metadata } from 'next';
import PricingPage from './pricing-client';

export const metadata: Metadata = {
  title: 'Pricing — Sprout Track',
  description:
    'Hosted baby tracking for $2.99 a month after a 14-day free trial, or $19.99 once for lifetime access. Every plan includes every feature, unlimited caretakers, and unlimited babies. Self-hosting is free forever.',
};

export default PricingPage;
