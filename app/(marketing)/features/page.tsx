import type { Metadata } from 'next';
import FeaturesPage from './features-client';

export const metadata: Metadata = {
  title: 'Features — Sprout Track',
  description:
    'Everything a newborn throws at you, one place to write it down. Sixteen kinds of entries — sleep, feeds, diapers, pumping, medicine, vaccines, solid foods, and more — shared with everyone who helps.',
};

export default FeaturesPage;
